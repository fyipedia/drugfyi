# Smithery configuration for drugfyi
# https://smithery.ai/docs/build/project-config
startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
  commandFunction: |-
    config => ({ command: 'uvx', args: ['--from', 'drugfyi[mcp]', 'python', '-m', 'drugfyi.mcp_server'] })
